# Deploy key (private key must stay secret)
deploy_key

# MuseScore build artifacts (rebuild from source)
MuseScore/build/

# Python virtual environment (recreate with: python3 -m venv venv && venv/bin/pip install music21)
venv/
__pycache__/
*.pyc

# Analysis output files
*_analyzed.musicxml

# Audio files (copyrighted; don't commit). The directory itself is tracked via .gitkeep
audio/*
!audio/.gitkeep

# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~
.vscode/
.idea/

# Environment
.env
.env.local
