Patches are auto-generated from modifications to the MuseScore submodule.

- `save-patches.sh` exports uncommitted changes from `MuseScore/` into numbered `.patch` files here
- `apply-patches.sh` applies all patches in order after a fresh clone + submodule init
